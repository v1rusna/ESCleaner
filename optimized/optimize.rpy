init -9999:
    $ persistent.nofilters = False
    
    # Отключить режим разработчика
    $ config.developer = False
    # Увеличить размер кэша изображений
    # $ config.image_cache_size = 128 #64 - 128 - 256
    # Предзагрузка следующих изображений
    # $ config.predict_statements = True
    # $ config.autosave_on_choice = False
    # $ config.rollback_enabled = False
    # Ускорить переходы
    $ config.fast_skipping = True

    # Уменьшить буферы аудио
    # $ config.sound_buffer_size = 1024
    # $ config.voice_buffer_size = 1024
